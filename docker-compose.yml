version: "3.8"

services:
  waysnip:
    image: '${DOCKER_IMAGE_PREFIX?Variable not set}waysnip:rolling'
    build:
      context: .
      dockerfile: dockerfiles/waysnip.dockerfile
      args:
        DOCKER_IMAGE_PREFIX: ${DOCKER_IMAGE_PREFIX?Variable not set}
    networks:
      - traefik-public
    env_file:
      - ./.env

networks:
  traefik-public:
    external: true
